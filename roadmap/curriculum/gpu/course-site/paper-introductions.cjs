// Publication introductions. Original notes and reading records stay unchanged.
module.exports = {
  "20": "在线 Softmax 在读取新数据块时，同时更新最大值和指数和。不同块使用不同指数基准，合并前先重标定，才能保持与整段归一化相同的数学结果。",
  "21": "并行归约先在局部计算统计量，再合并为整体结果。运算的单位元、合并顺序和线程间的数据交换，决定尾部处理、数值误差和执行成本。",
  "22": "FlashAttention 将 Attention 的矩阵乘与在线 Softmax 组织在数据 tile 内，减少完整分数和概率矩阵的显存读写。分块改变数据驻留方式，归一化仍覆盖每个 Query 可见的全部 Key。",
  "23": "FlashAttention-2 沿用分块 Attention，进一步调整线程块并行、warp 分工和归一化操作。理解这些改动，需要把公式中的行归约对应到 tile、累加器和线程间的数据交换。",
  "24": "MQA 让全部 Query head 共享一组 KV，GQA 则让若干 Query head 共享一组。分组数改变缓存容量与表达能力，也影响已有 MHA checkpoint 的转换方式。",
  "25": "MLA 将历史 Key/Value 的主要内容保存为低维 latent。线性投影可以吸收到 Query 和输出路径中，Decode 因而能够直接消费压缩缓存；位置编码分支需要单独处理。",
  "26": "DSA 先用索引器选择历史位置，再对选中的 KV 计算 Attention。索引评分、主 Attention 权重和持久缓存分别承担筛选、加权与历史保存的职责。",
  "27": "线性注意力把历史信息累积到固定维度的状态中，Decode 通过状态递推处理新 token。Gated DeltaNet 在更新中使用门控与纠错项，其存储和计算路径与显式 KV Attention 不同。",
  "28": "可编程注意力允许自定义分数变换和可见性规则。规则怎样表达、怎样映射到分块计算，以及后端怎样调度，是三个需要分别检查的层次。",
  "29": "低精度 Attention 改变数值表示，结构化稀疏与复用改变访问范围。比较这些方法时，同时记录近似误差、保留的上下文和实际数据流量。",
  "30": "模型配置可以拆成三笔账：权重需要多少存储，历史 KV 如何增长，一次前向包含多少计算。容量、单步访问量和 FLOPs 分别计算，再结合 batch 与硬件分析瓶颈。",
  "31": "DeepSeek-V4 使用压缩与稀疏全局注意力，同时保留局部处理。缓存条目的形成方式、选择规则和层间数据流共同决定长上下文的成本。",
  "32": "MoE 根据 token 选择专家。专家矩阵计算之外，还包括路由、分派、数据重排与加权合并；小批次和负载不均会改变这些步骤的相对成本。",
  "33": "INT8 使用整数网格与 scale 表示数值，FP8 使用指数和尾数。格式选择影响动态范围、舍入误差、缓存布局和可用计算指令。",
  "34": "投机解码先产生候选，再由目标模型验证。接受与拒绝规则保证采样语义，草稿成本、连续接受长度和验证批次共同决定速度。",
  "35": "Prefill 与 Decode 可以分配到不同资源池，分别调整批次和调度策略。阶段之间需要传递 KV，收益取决于计算、容量、传输和排队成本。",
  "36": "PagedAttention 用页表把逻辑 KV 序列映射到非连续物理块，便于按需分配和共享前缀。分页改变存储管理，Attention 仍按逻辑序列计算。",
  "37": "优化器根据梯度更新参数，并维护动量或其他统计量。Adam、AdamW 和 Muon 对更新方向的处理不同，状态容量、数值精度与分片方式也随之变化。",
  "38": "ZeRO 将数据并行副本中的优化器状态、梯度和参数逐步分片。每个阶段节省不同的存储对象，也引入不同的通信与参数可用性约束。",
  "39": "长任务不断追加工具结果，Prefill 计算、历史缓存和缓存搬运会持续增长。V4.1 将 CED、跨层复用、低精度缓存和窗口重放结合起来，分别调整计算深度、缓存份数、条目大小与恢复成本。",
  "40": "预训练、生成训练样本和在线服务都执行矩阵乘与 Attention，但优化目标不同。模型规模影响长期成本，生成调度影响训练样本分布，工具和缓存状态又影响任务的实际等待时间。"
};

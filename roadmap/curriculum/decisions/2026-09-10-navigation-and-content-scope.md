# 导航联动与内容边界

用户指出顶部与侧栏未联动、子课程未折叠，并询问实际内容更新范围。

原因：此前顶部只有高亮与页面跳转，侧栏是两个分区的平铺链接，没有真正的可折叠章节树。修复将顶部、当前分区、当前章节、URL与小节锚点统一处理，侧栏从实际正文标题生成子目录。

## 内容事实

- 已有网页正文：GPU基础五章，完整算子体系总览，访存与布局首章，共7页。其中6页为详细教程，1页为总览。
- 完整算子总览列出九族，不代表九族全部详细正文已生成。新算子正文目前只到访存与布局；已有MatMul、Softmax等代码、旧课和实验继续保留。
- 极致优化、量化、模型GPU分析和系统落地存在路线/旧笔记/部分实测，不能据此说整套课程已完成。
- 内容生成与用户学习状态分开。PATH/NOW当前实践和论文焦点未因本次导航修改而改变。

## 验证口径

浏览器工具连接失败。新增jsdom测试用于执行app.js的状态与交互逻辑，覆盖顶部/侧栏切换、折叠、小节锚点、搜索与历史。坐标和滚动为模拟，不能替代真实浏览器布局或截图验收。构建、内容检查与HTTP可访问性另行验证。

修复后DOM测试已通过，包括发现并修正顶部双handler覆盖分区记忆、非法chapter与URL不一致两处问题。新增测试只作为开发依赖，不进入网页运行资源。实际课程正文范围如上，未继续生成新的算子课程。

# 五主体课程与网页重点优化

日期：2026-09-10。

## 已确认

用户重新指出此前五主体方案才是顶层基准，并确认按优化继续生成。五主体为：NPU→GPU架构与性能迁移、完整GPU算子体系、算子极致性能优化、低精度/量化与模型GPU分析、真实系统落地。论文线独立，vLLM是落地与加分项，不作为核心目标。

之前提出的基础六章只是一部分算子教学组织，不替代完整九族覆盖；Scan/Histogram等需要补入但不能挤掉高级GEMM、Prefill/Decode、量化、MoE、采样与KV操作。极致优化保留独立项目深度，与各算子实现紧邻衔接。

## 本次落实范围

- 总路线按五主体组织；PATH/NOW仅修正八章/五章等目录口径，不修改用户完成状态、当前Softmax实践或MLA论文焦点。
- 完整算子总入口保留九族，生成访存与布局首章；平台题目、原始solve、服务器包装与性能证据分开。
- 网页加入核心结论、边界条件、实践复盘样式，关键代码行高亮与行号；基础五章及算子总览/首章统一入口，旧chapter=1..5链接继续可用。
- 图解分别标明请求层数学模型、buffer依赖示意、Nsys原始数据比较，不把示意伪装成实测截图。
- GPU基础补统一地址/统一内存/映射内存、NPU经验迁移方法、Nsys逐行筛样本与数值/工具验证合同。

## 来源与保留

主教材为本地CUDA Programming Guide 13.3；在线文档已更新，不能把最新页面继续标为13.3。网页参考用户指定的AIInfraGuide学习路线正文，不用其首页替代教程内页。

原始代码、旧教程、讨论和性能日志均原位保留。真实实验在正文中带GPU/shape/精度/批次；用户原话与整理后的解释不混作同一引述。学习状态和写作/验证边界放在维护记录，不出现在教程页面。

本次不commit/push、不运行远端GPU。静态构建、源码与图表核对不能冒充CUDA编译、真实GPU运行或浏览器截图验收。

## 实际交付与检查

- 完整算子首章包含Copy、view/materialize、stride单位、16B对齐与尾部、转置线程映射、gather/scatter和packing边界、真实baseline/缓存口径、LeetGPU入口与服务器命令；新增copy_alignment.cu，复用既有transpose例程。
- 修正写作草稿中的NumPy stride单位、转置view步长、Triton逻辑tile与线程的混淆、错误相对路径和公式过度转义。原始用户代码没有改写。
- 三张SVG分别为sector覆盖模型、buffer依赖示意和已有Nsys均值比较；图中不混用示意时间与实测时间。
- 构建生成7页、排版67处公式；脚本检查重点提示、关键行、源码复制一致性、链接和数学索引通过；页面HTTP200。没有浏览器视觉/交互验收，没有nvcc或GPU运行。
- 一个写作worker在超过等待窗口后停止，主Agent完成首章剩余内容与明确校对，无重叠写入。
- 其他算子族保留在总入口及未来课程范围，没有将它们标记为正文或实践已完成。五主体定位与独立论文线不变。

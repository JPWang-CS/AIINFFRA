# 旧代码直接融入课程

用户指出lesson文件链接无法满足阅读需要，要求把旧实现、代码片段和解释直接融入当前课程，而不是只提供跳转。

本次针对GPU执行章和存储章中的GEMM引用：使用solutions/cuda/gemm/naive_float.cu与tiled_fp16.cu，直接展示输出映射、归约地址、FP32累加、shared装载与两次同步。两份接口的M/N/K命名不同，必须逐份定义；源码中的历史优化推断不直接当作已验证事实。

原文件继续保留，不为教学或同步检查改写用户实现。原样摘录与教学摘编区分；构建增加source-check注释校验，防止代码与讲解版本悄悄分离。文件链接只作附属出处，正文应当能独立阅读。

没有改动学习完成状态，没有CUDA实卡验证，没有提交或推送。
